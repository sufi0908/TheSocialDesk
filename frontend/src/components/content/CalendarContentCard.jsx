import React from 'react';
import { CalendarContentCard as CalendarContentCardCore } from '../calendar/CalendarContentCard';

export const CalendarContentCard = (props) => {
  return <CalendarContentCardCore {...props} />;
};

export default CalendarContentCard;
